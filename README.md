# sentinel-core
 A header-only C++23 library with demo that detects when a user switches between active applications on Windows and Linux, then transmits that "event" to a secondary "Logger" process via a transport layer.
